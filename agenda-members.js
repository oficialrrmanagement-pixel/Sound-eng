(()=>{
const q=id=>document.getElementById(id);let selected='ALL';
function activePeople(){return (team||[]).filter(p=>p.active!==false)}
function renderTabs(){const host=q('agendaMembers');if(!host)return;const people=activePeople();host.innerHTML=`<button type="button" class="secondary tiny ${selected==='ALL'?'active':''}" data-agenda-member="ALL">Geral</button>`+people.map(p=>`<button type="button" class="secondary tiny ${selected===String(p.id)?'active':''}" data-agenda-member="${p.id}">${esc(p.full_name||p.email||'Membro')}</button>`).join('');host.querySelectorAll('[data-agenda-member]').forEach(b=>b.onclick=()=>{selected=b.dataset.agendaMember;renderTabs();apply()})}
function apply(){const host=q('concerts');if(!host)return;const cards=[...host.querySelectorAll(':scope > .item')];let visible=0;cards.forEach((card,i)=>{const c=(concerts||[])[i];if(!c)return;const byMember=selected==='ALL'||String(c.technician_id)===selected;const alreadyHidden=card.style.display==='none';const show=byMember&&!c.closed&&!alreadyHidden;card.dataset.memberHidden=byMember?'0':'1';if(!byMember)card.style.display='none';else if(card.dataset.filterHidden!=='1')card.style.display='';if(byMember&&card.style.display!=='none')visible++});const title=document.querySelector('#agenda .title-row h2');if(title){const p=activePeople().find(x=>String(x.id)===selected);title.textContent=selected==='ALL'?'Agenda':`Agenda · ${p?.full_name||'Membro'}`}}
function hookAgendaClick(){const agendaBtn=document.querySelector('#nav button[data-page="agenda"]:not([data-agenda-filter])');agendaBtn?.addEventListener('click',()=>{selected='ALL';renderTabs();setTimeout(apply,0)})}
function patchLoadAll(){if(window.__agendaMembersPatched||typeof window.loadAll!=='function')return;const old=window.loadAll;window.loadAll=async function(...args){const r=await old.apply(this,args);renderTabs();setTimeout(apply,0);return r};window.__agendaMembersPatched=true}
addEventListener('load',()=>{hookAgendaClick();patchLoadAll();setTimeout(()=>{renderTabs();apply()},500)});
})();